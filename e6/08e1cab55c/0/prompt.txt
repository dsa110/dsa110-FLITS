Build the freya CHIME scintillation inputs and run the CHIME-side measurement for dsa110-FLITS. Work in a NEW worktree: git worktree add ~/Developer/scratch/worktrees/flits-chime-scint -b feat/freya-chime-scint-config origin/main (from this canonical clone). Do NOT edit the canonical clone's tracked files; do NOT open a PR or merge; push the branch when done.

INPUTS (already staged, md5-verified): ~/Data/Faber2026/dsa110/upchan_codetections/freya_chime_upchan.npy — float32 (57024 channels, 437 time bins), Stokes-I dynamic spectrum, ~92% finite (NaN = masked RFI), burst at time bin ~250 (z≈8.6 in 600–800 MHz band-sum, ~13–23 bins >5σ, fades toward 400 MHz); freya_chime_freq.npy — 57024 fine-channel centres in MHz, 400.195–799.805, df=6.1036 kHz (upchan U=64). Time resolution dt = 2*2.56e-6*64 = 3.2768e-4 s exactly (CHIME baseband convention); times_s = arange(437)*dt. DM already coherently dedispersed at 912.4 — do NOT dedisperse again.

TASK 1 — build npz pair in the casey pattern (casey_chime.npz full band + casey_chime_hi.npz band-crop, in ~/Data/Faber2026/dsa110/scintillation-data/ and mirrored in scintillation/data/ of the repo checkouts):
- freya_chime.npz: full band. freya_chime_hi.npz: 600–800 MHz crop (the measurement band: burst is bright there and the expected scintle 38.8 kHz@600→138 kHz@800 is well-resolved by df=6.1 kHz, whereas at 400 MHz the expected scale ≈ df and is unresolvable).
- npz keys: power_2d, frequencies_mhz, times_s. CRITICAL LOADER TRAP: scintillation/scint_analysis/core.py DynamicSpectrum.from_numpy_file does np.flip(data['power_2d'], axis=0) and takes frequencies_mhz as-is. INSPECT the existing scintillation/data/casey_chime.npz (keys, frequency ordering, which rows hold the burst) and mirror its exact storage convention. VERIFY after writing: load via DynamicSpectrum.from_numpy_file and confirm the burst power sits at the correct frequencies (600–800 MHz) and time bin ~250 — if the band looks inverted, you got the flip wrong.
- Write the npz files to ~/Data/Faber2026/dsa110/scintillation-data/ and copy (or symlink, matching how casey files exist as real files) into BOTH the canonical clone's scintillation/data/ and the worktree's scintillation/data/ (dir is gitignored; data never gets committed).

TASK 2 — config: create scintillation/configs/bursts/freya_chime_hi.yaml in the worktree using scintillation/configs/bursts/casey_chime_hi.yaml as the template (read it first; keep its structure/comment style). Changes: burst_id freya; input_data_path ${FLITS_ROOT}/scintillation/data/freya_chime_hi.npz; telescope chime; manual_burst_window/manual_noise_window derived from the actual freya profile in the CROPPED npz (find the >3sigma span around bin ~250; noise window well before it); acf.max_lag_mhz 5.0; fitting.fit_lagrange_mhz 1.0; reference_frequency_mhz ~700 (crop centre); baseline_subtraction enable true poly_order 1. NOTE the #118 contract in the runner: baseline subtraction silently skips unless the off-pulse window exceeds 50 bins — make the noise window comfortably >50 bins. Add a freya_chime.yaml for the full-band npz too (casey_chime.yaml as template) but the hi config is the measurement config. Comment the expected scale: NE2025 MW floor ~38.8 kHz@600 MHz scaling nu^4.4 to ~138 kHz@800 (from analysis/scattering-refit-2026-06/scint_census; freya floor 1.6421 MHz@1.405 GHz).

TASK 3 — run the CHIME-side measurement with the #118-hardened CLI from the WORKTREE root (its code == origin/main with the fail-closed fit contract): cd into the worktree, then conda run -n flits python -m scintillation.scint_analysis.freya_scintillation scintillation/configs/bursts/freya_chime_hi.yaml --out scintillation/plots/freya_chime 2>&1 | tee a log. cwd shadows the editable install so the worktree code runs — verify by printing the module __file__ first. Inspect the JSON output and figures. If the Lorentzian fit fails closed (success=false: railed gamma or non-finite covariance), that is a VALID scientific outcome — report it verbatim as the contract firing, do NOT tweak parameters to force success=true. If it succeeds, report delta_nu_mhz ± err, modulation_index, and the structure-function cross-check value. Optionally also run the full-band config as a diagnostic and report how it differs.

TASK 4 — commit ONLY the two yaml configs (plus nothing else) on the branch with a conventional message referencing 'freya CHIME scintillation config (upchan U=64 product)', push the branch to origin. Leave the worktree in place.

REPORT (this is your final message, written as raw data): npz storage convention used (flip details) + verification evidence; window choices; the measurement JSON verbatim key numbers (success, message, delta_nu_mhz, delta_nu_err_mhz, modulation_index, structure delta_nu_mhz); figure paths; branch name + commit SHA pushed; any deviations. Epistemic framing: any delta_nu_d you report is a first-look current-model result pending figure inspection by the owner — say so.