Review PR #114 on jakobtfaber/dsa110-FLITS (this checkout, branch feat/104-freya-joint-production-fit; diff = git diff origin/main...HEAD, code commits + ledger tails). Scope per issue #104's agent brief:
(1) local_runs/run_joint_fit.py — gain-recovery block: FRBParams(..., alpha=a_m) TypeErrored post-ADR-0006; fix passes beta=p['beta']. Verify correct/complete (no other alpha= FRBParams constructions on the driver path), p['beta'] present for every path reaching the block, no behavior change beyond crash avoidance.
(2) analysis/scattering-refit-2026-06/joint_ppc.py — same fix class: beta-native FRBParams with beta_from_alpha_thin_screen fallback for old alpha-only JSONs; derived alpha now read from d['alpha']['median'] (top-level) instead of percentiles; optional variant tag argv. Verify fallback semantics (raises for alpha<=2? acceptable?), tag plumbing, no regression for the legacy untagged/non-gain path.
(3) Artifacts: local_runs/freya_joint_fit_sharedzeta.json + freya_joint_samples_sharedzeta.npz + freya_joint_ppc.{json,png} + figures.manifest.json/figures.review.json. Sanity-check the JSON's internal consistency (beta percentiles vs derived alpha via alpha=2b/(b-2); alpha_bounds [4,6] consistent with beta prior [3,4); ncall plausible) and that the manifest/review pair matches the committed png.
(4) Physics kernel untouched — verify.
Verdict: APPROVE or REQUEST_CHANGES with file:line findings and severities.