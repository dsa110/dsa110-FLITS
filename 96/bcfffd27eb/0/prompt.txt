Review the code commit on PR #114 (jakobtfaber/dsa110-FLITS, this checkout, branch feat/104-freya-joint-production-fit; diff = git diff origin/main...HEAD, one code commit + a ledger tail). Scope: local_runs/run_joint_fit.py gain-recovery block previously constructed FRBParams(..., alpha=a_m, ...) which TypeErrors post-ADR-0006 (alpha is a derived property, not an init field); the fix passes beta=p['beta'] (the sampled beta median) instead, for both pC and pD. Verify: (1) the fix is correct and complete — no other alpha= FRBParams constructions remain in the driver or its call path; (2) p['beta'] exists for every path that reaches this block (marginalize_gain / marginalize_gain_gp / shared_zeta, not multi) given JOINT_PARAM_NAMES* layouts in scattering/scat_analysis/burstfit_joint.py; (3) the claim that beta median is the alpha median's preimage under the monotone map (weighted-percentile semantics in _weighted_percentiles/_append_derived_alpha_percentiles); (4) no behavior change beyond crash avoidance; kernel untouched. Verdict APPROVE or REQUEST_CHANGES with file:line findings.